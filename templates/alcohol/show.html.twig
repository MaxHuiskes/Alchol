{% extends 'base.html.twig' %}

{% block title %}ingridients{% endblock %}

{% block body %}
    <h1>ingridients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alcohol.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ alcohol.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ alcohol.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alcohol_index') }}" class="back-link" >Back to list</a>

    <a href="{{ path('app_alcohol_edit', {'id': alcohol.id}) }}" class="btn">edit</a>

    {{ include('alcohol/_delete_form.html.twig') }}
{% endblock %}
