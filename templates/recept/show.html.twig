{% extends 'base.html.twig' %}

{% block title %}Recipe{% endblock %}

{% block body %}
    <h1>Recipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recept.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recept.name }}</td>
            </tr>
            <tr>
                <th>Recipe</th>
                <td>{{ recept.recept }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recept_index') }}" class="back-link">Back to list</a>

    <a href="{{ path('app_recept_edit', {'id': recept.id}) }}">edit</a>

    {{ include('recept/_delete_form.html.twig') }}
{% endblock %}
