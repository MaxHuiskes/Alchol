<form method="post" action="{{ path('app_alchole_delete', {'id': alchole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alchole.id) }}">
    <button class="btn">Delete</button>
</form>
