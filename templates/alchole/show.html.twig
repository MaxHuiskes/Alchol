{% extends 'base.html.twig' %}

{% block title %}Alchole{% endblock %}

{% block body %}
    <h1>Alchole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alchole.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ alchole.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ alchole.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alchole_index') }}" class="back-link" >Back to list</a>

    <a href="{{ path('app_alchole_edit', {'id': alchole.id}) }}" class="btn">edit</a>

    {{ include('alchole/_delete_form.html.twig') }}
{% endblock %}
