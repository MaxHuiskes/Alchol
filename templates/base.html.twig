<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="google-site-verification" content="s7KMXpIYh9GEp0lU76_KK1lfGhxb_jUMCgzmJ2ZSKEs" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        <style>
            body {
                margin: 0;
                font-family: system-ui, sans-serif;
                background-color: #FFF8F0;
                color: #4B2E2E;
            }

            nav {
                background-color: #D97706;
                padding: 0.5rem 1rem;
            }

            nav ul {
                margin: 0;
                padding: 0;
                list-style: none;
                display: flex;
                gap: 1rem;
                background-color: #D97706;
            }

            nav li {
                display: inline;
                background-color: #D97706;
            }

            nav a {
                color: #FFF8F0;
                text-decoration: none;
                padding: 0.5rem 0.75rem;
                border-radius: 4px;
                transition: background-color 0.2s ease;
                
            }

            nav a:hover {
                background-color: #FFD700;
                color: #4B2E2E;
            }

            main {
                max-width: 1000px;
                margin: 2rem auto;
                padding: 1rem;
                background: white;
                border-radius: 8px;
                box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            }

         h1 {
            margin-bottom: 1rem;
        }

        table.table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 1.5rem;
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 6px rgba(0,0,0,0.05);
        }

        table.table th,
        table.table td {
            padding: 0.75rem 1rem;
            text-align: left;
            border-bottom: 1px solid #e0e0e0;
        }

        table.table thead {
            background-color: #f7f7f7;
        }

        table.table tr:hover {
            background-color: #fafafa;
        }

        a.btn, button.btn {
            display: inline-block;
            padding: 0.4rem 0.8rem;
            border-radius: 4px;
            background-color: #D97706;
            color: #FFF8F0;
            text-decoration: none;
            font-size: 0.9rem;
            transition: background 0.2s ease;
        }

        a.btn:hover, button.btn:hover {
            background-color: #FFD700;
            color: #4B2E2E;
        }

        a.btn-secondary {
            background-color: #FFD700;
            color: #4B2E2E;
        }

        a.btn-secondary:hover {
            background-color: #D97706;
            color: #FFF8F0;
        }

        h1 {
            margin-bottom: 1rem;
        }

        form {
            background: white;
            padding: 1.5rem;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.05);
            max-width: 500px;
        }

        form div {
            margin-bottom: 1rem;
        }

        label {
            font-weight: bold;
            display: block;
            margin-bottom: 0.5rem;
        }

        input[type="text"],
        input[type="password"],
        select,
        textarea {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1rem;
            box-sizing: border-box;
        }

        a.back-link {
            display: inline-block;
            margin-top: 1rem;
            text-decoration: none;
            color: #D97706;
        }

        a.back-link:hover {
            color: #FFD700;
            text-decoration: underline;
        }
        .cocktail-list {
            list-style: none;
            padding: 0;
            margin: 0;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
        }

        .cocktail-list li {
            background: #FFF8F0;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(77, 46, 46, 0.1);
            transition: transform 0.15s ease, box-shadow 0.15s ease;
            border: 1px solid #D97706;
        }

        .cocktail-list li:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 12px rgba(77, 46, 46, 0.15);
            background: #FFD700;
        }

        .cocktail-list a {
            display: block;
            padding: 1rem;
            text-decoration: none;
            color: #D97706;
            font-weight: bold;
            text-align: center;
        }

        .cocktail-list a:hover {
            color: #4B2E2E;
        }

        .no-cocktails {
            padding: 1rem;
            text-align: center;
            color: #888;
            font-style: italic;
        }

        </style>
    {% endblock %}

    {% block javascripts %}
    {% endblock %}
</head>
<body>

        <nav>
            <ul>
                <li><a href="{{ path('cocktail_index') }}">Cocktails finder</a></li>
                {% if not app.user %}
                    <li><a href="{{ path('cocktail_list') }}">Cocktail list</a></li>
                {% endif %}
                {% if app.user %}
                    <li><a href="{{ path('app_recept_index') }}">Recipe</a></li>
                    <li><a href="{{ path('app_alcohol_index') }}">Ingredients</a></li>
                    <li><a href="{{ path('app_logout') }}">Logout</a></li>
                {% endif %}
            </ul>
        </nav>

    <main>
        {% block body %}{% endblock %}
    </main>

</body>
</html>
